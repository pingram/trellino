<h3>Boards Index</h3>

<% boards.each(function(board) { %>
  <a href="#boards/<%= board.id %>">
    <%= board.escape('title') %>
  </a>
  <button class='delete-board' data-id="<%= board.id %>">
    Delete
  </button>
  <br>
<% }) %>

<br>

<a href="#boards/new">Create new board</a>