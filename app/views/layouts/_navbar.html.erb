<nav class="navbar navbar-default">
  <div class="container">

    <% if logged_in? %>
    <ul class="nav navbar-nav">
      <li class="dropdown">
        <a href="#" class="dropdown-toggle" data-toggle="dropdown">Boards</a>
        <ul class="dropdown-menu">
        <% current_user.boards.each do |board| %>
          <li><a href="#boards/<%= board.id %>"><%= board.title %></a></li>
        <% end %>
          <li class="divider"></li>
          <li><a href="#">All Boards</a></li>
        </ul>
      </li>
    </ul>
    <% end %>

    <ul class="navbar-header">
      <a class="navbar-brand" href="#">Trellino</a>
    </ul>

    <ul class="nav navbar-right">
      <li>
        <% if logged_in? %>
          <%= button_to "Log out", session_url, { method: :delete, class: "btn btn-md btn-warning" } %>
        <% else %>
          <%= link_to "Sign In", new_session_url, { class: "btn btn-default" } %>
        <% end %>
      </li>
    </ul>

  </div>
</nav>